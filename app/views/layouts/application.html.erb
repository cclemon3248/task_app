<!DOCTYPE html>
<html>
  <head>
    <title>TaskApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <h1>Ruby on Rails スケジュール管理アプリ</h1>
    </header>
    <main><%= yield %></main>
    <footer>
      <h1>
        現在の日時 : 
        <%= "#{Date.today.year}年#{sprintf("%02d", Date.today.month)}月#{sprintf("%02d", Date.today.day)}日" %>
        &nbsp;&nbsp;
        スケジュール合計 :
        <%= @blogs.count %>
      </h1>
    </footer>
  </body>
</html>